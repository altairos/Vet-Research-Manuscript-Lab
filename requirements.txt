# Runtime dependencies for Streamlit Cloud deployment.
# Streamlit Cloud reads this file automatically.
# Install the project itself (src/ layout) so `vet_manuscript_lab` is importable.
-e .
alembic>=1.14,<2
langgraph>=1.0,<2
langgraph-checkpoint-sqlite>=2,<4
pymupdf>=1.24,<2
pyzotero>=1.5,<2
sqlalchemy>=2.0,<3
streamlit>=1.58.0,<1.59.0  # pin to match local dev; CSS selectors target internal DOM that changes between versions
# Architecture Decision Records

ADRs capture decisions that affect system boundaries, scientific governance, or
cross-cutting implementation. Accepted records are immutable; a changed decision
is documented by a new ADR that supersedes the old one.

Status values: `proposed`, `accepted`, `deprecated`, `superseded`.

